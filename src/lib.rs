mod health_statistic;
mod low_power_game;
mod role_playing_game;
mod struct_logs;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
